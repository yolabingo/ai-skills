---
phase: 02-first-skills
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Port gh-intercept plugin into ai-skills repo as first skill, with paths updated for marketplace distribution.
</objective>

<context>
@.paul/PROJECT.md
@.claude-plugin/marketplace.json
@.claude-plugin/plugin.json
</context>

<acceptance_criteria>
## AC-1: gh-intercept plugin ported with correct structure and no hardcoded paths
Given gh-intercept exists under skills/gh-intercept/
When all files are present (hooks, scripts, skill, plugin configs)
Then no hardcoded cache paths remain in SKILL.md — all use ${CLAUDE_PLUGIN_ROOT}
And marketplace.json lists gh-intercept as a plugin
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Port gh-intercept plugin to skills/gh-intercept/</name>
  <files>
    skills/gh-intercept/.claude-plugin/plugin.json,
    skills/gh-intercept/.claude-plugin/marketplace.json,
    skills/gh-intercept/hooks/hooks.json,
    skills/gh-intercept/hooks/notify-cached.sh,
    skills/gh-intercept/hooks/notify-cached-bash.sh,
    skills/gh-intercept/hooks/clone-on-fetch.sh,
    skills/gh-intercept/hooks/clone-on-gh-api.sh,
    skills/gh-intercept/scripts/gh-cache.sh,
    skills/gh-intercept/skills/gh-search/SKILL.md,
    .claude-plugin/marketplace.json
  </files>
  <action>
    1. Copy entire gh-intercept structure to skills/gh-intercept/
    2. Fix SKILL.md: replace all hardcoded paths like
       "/Users/toddj/.claude/plugins/cache/gh-intercept/gh-intercept/1.0.0/scripts/gh-cache.sh"
       with relative references using ${CLAUDE_PLUGIN_ROOT}:
       bash "${CLAUDE_PLUGIN_ROOT}/scripts/gh-cache.sh"
    3. Update root .claude-plugin/marketplace.json to list gh-intercept
    4. Remove skills/.gitkeep (no longer needed)
    5. Hook scripts already use ${CLAUDE_PLUGIN_ROOT} — verify, don't break
    Avoid: Modifying hook logic or gh-cache.sh behavior
  </action>
  <verify>grep -r "Users/toddj" skills/gh-intercept/ (should return nothing)</verify>
  <done>AC-1 satisfied: gh-intercept ported, no hardcoded paths, marketplace updated</done>
</task>
</tasks>

<output>
After completion, create compressed SUMMARY.md
</output>
