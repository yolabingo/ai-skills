---
phase: 01-repository-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - .claude-plugin/plugin.json
  - package.json
  - .gitignore
  - skills/.gitkeep
autonomous: true
---

<objective>
## Goal
Create valid Claude plugin marketplace structure and npm package config so the repo is structurally installable as a marketplace.

## Purpose
Foundation for all skill distribution. Without marketplace.json and plugin.json, skills can't be discovered or installed via `claude plugin marketplace add`.

## Output
- `.claude-plugin/marketplace.json` — marketplace registry
- `.claude-plugin/plugin.json` — plugin metadata
- `package.json` — npm package config with bin entry for future CLI
- `skills/` — directory for skill definitions
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Source Files
@README.md
@LICENSE
</context>

<acceptance_criteria>

## AC-1: Valid marketplace structure
```gherkin
Given the repo has .claude-plugin/ directory
When marketplace.json and plugin.json are present
Then both files contain valid JSON matching Claude plugin marketplace schema
```

## AC-2: npm package config
```gherkin
Given package.json exists at repo root
When npm validates the package
Then npm pack runs without errors and package has correct name, version, and bin entry
```

## AC-3: Skills directory exists
```gherkin
Given the repo structure is in place
When a contributor looks for where to add skills
Then skills/ directory exists with clear purpose
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create Claude plugin marketplace config</name>
  <files>.claude-plugin/marketplace.json, .claude-plugin/plugin.json</files>
  <action>
    Create `.claude-plugin/` directory with two files:

    marketplace.json:
    - name: "ai-skills"
    - owner: { name: "toddj", url: "https://github.com/toddj" }
    - plugins: empty array (skills added in Phase 2)
    - Follow schema from slavingia/skills, trailofbits/skills examples

    plugin.json:
    - name: "ai-skills"
    - description: "Curated AI coding skills for Claude Code, Codex, and other assistants"
    - version: "0.1.0"
    - skills: {} (populated when skills added)

    Avoid: Inventing schema fields not in established marketplace format.
  </action>
  <verify>cat .claude-plugin/marketplace.json | python3 -m json.tool && cat .claude-plugin/plugin.json | python3 -m json.tool</verify>
  <done>AC-1 satisfied: Both files are valid JSON matching marketplace schema</done>
</task>

<task type="auto">
  <name>Task 2: Create package.json and skills directory</name>
  <files>package.json, skills/.gitkeep</files>
  <action>
    Create package.json at repo root:
    - name: "ai-skills"
    - version: "0.1.0"
    - description: "Curated AI coding skills for Claude Code, Codex, and other assistants"
    - license: match LICENSE file (MIT or whatever exists)
    - bin: { "ai-skills": "./cli.js" } (cli.js created in later phase)
    - repository: { type: "git", url: "https://github.com/toddj/ai-skills" }
    - keywords: ["claude-code", "codex", "ai-skills", "plugin", "marketplace"]
    - files: ["skills/", ".claude-plugin/", "cli.js"]

    Create skills/.gitkeep to preserve empty directory in git.

    Avoid: Adding dependencies yet — CLI implementation comes later.
    Avoid: Running npm install — no deps to install.
  </action>
  <verify>python3 -c "import json; json.load(open('package.json'))" && ls skills/.gitkeep</verify>
  <done>AC-2 and AC-3 satisfied: package.json valid, skills/ directory exists</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- LICENSE (existing file, don't modify)
- .paul/* (project management files)
- .git/* (git internals)

## SCOPE LIMITS
- No CLI implementation (Phase 2+)
- No actual skills yet (Phase 2)
- No README updates (Plan 01-02)
- No Codex config (Phase 3)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] .claude-plugin/marketplace.json is valid JSON
- [ ] .claude-plugin/plugin.json is valid JSON
- [ ] package.json is valid JSON with correct fields
- [ ] skills/ directory exists
- [ ] No existing files modified (LICENSE, README untouched)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Repo structurally valid as Claude plugin marketplace (even if empty)
- package.json ready for future npx CLI
</success_criteria>

<output>
After completion, create `.paul/phases/01-repository-foundation/01-01-SUMMARY.md`
</output>
